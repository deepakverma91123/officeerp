import { SupplierInformation } from './supplier-information';

describe('SupplierInformation', () => {
  it('should create an instance', () => {
    expect(new SupplierInformation()).toBeTruthy();
  });
});
